#pragma once
#include <queue>
#include <list>
#include <vector>
#include <map>
#include <optional>


class  Node
{
private:
    bool m_Flag = false;
    
public:
    Node() {}

    explicit Node(const Node& rhs) {}
    explicit Node(const int& val){}
    
};
